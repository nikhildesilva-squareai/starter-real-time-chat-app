export const metadata = { title: "Real-time Chat App", description: "Square 1 AI Project" };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>;
}
